{
  "mcpServers": {
    "design-space": {
      "command": "node",
      "args": ["${CLAUDE_PROJECT_DIR}/mcp-server/dist/index.mjs"],
      "env": {
        "DESIGN_SPACE_ROOT": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
